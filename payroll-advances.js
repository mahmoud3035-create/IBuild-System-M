'use strict';

// Legacy compatibility module intentionally contains no UI injection.
// Advance management lives exclusively on /payroll-advances.html.
// Payroll receives the selected monthly deduction through the dedicated API.
module.exports = {};
